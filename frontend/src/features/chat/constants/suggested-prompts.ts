export const SUGGESTED_PROMPTS: readonly string[] = [
  "Which internal employees know Python?",
  "Which department has the most employees?",
  "Find popular FastAPI repositories on GitHub.",
  "Recommend a GitHub project for employees with FastAPI experience.",
  "What is the difference between FastAPI and Flask?",
];

/** Matches the backend AskRequest question max_length. */
export const QUESTION_MAX_LENGTH = 10000;

/** Matches the backend session title max_length. */
export const SESSION_TITLE_MAX_LENGTH = 150;
